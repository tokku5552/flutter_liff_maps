name: flutter_liff_maps
description: A new Flutter project.
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  cloud_firestore: ^4.7.1
  cupertino_icons: ^1.0.5
  firebase_core: ^2.13.0
  flutter:
    sdk: flutter
  # LINE Login まわりの開発中に使用する
  # 最終的には LIFF で LINE Login 相当のことをするので消す。
  flutter_line_sdk: ^2.3.2
  geoflutterfire_plus: ^0.0.11
  google_maps_flutter: ^2.2.8
  google_maps_flutter_web: ^0.4.0+9
  rxdart: ^0.27.7

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
